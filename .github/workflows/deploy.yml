name: Deploy Flask App to Azure Web App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Delete old wwwroot files via Kudu API
      env:
        DEPLOY_USER: ${{ secrets.AZURE_DEPLOY_USER }}
        DEPLOY_PASSWORD: ${{ secrets.AZURE_DEPLOY_PASSWORD }}
        APP_NAME: landki-bot-app
      run: |
        echo "Deleting old wwwroot content via Kudu API..."
        curl -X DELETE -u "$DEPLOY_USER:$DEPLOY_PASSWORD" \
        "https://${APP_NAME}.scm.azurewebsites.net/api/vfs/site/wwwroot/"

    - name: Deploy app.py + requirements.txt + runtime.txt
      uses: azure/webapps-deploy@v2
      with:
        app-name: landki-bot-app
        package: .
        clean: true
        enable-oryx-build: false
